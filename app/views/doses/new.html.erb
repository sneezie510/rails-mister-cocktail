<%= simple_form_for [@cocktail, @doses] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient_id, collection: Ingredients.all %>
  <%= f.submit "add a description", class: "btn btn-primary" %>
<% end %>
