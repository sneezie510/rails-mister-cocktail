<h1>All Cocktails</h1>

<div>
  <ul>
  <% @cocktails.each do |cocktail| %>
    <li><%= cocktail.name %>
    <%= link_to 'Show my cocktail', cocktail_path(cocktail) %></li>
      <% end %>
      <li><%= link_to 'Add a new drink', new_cocktail_path %></li>
    </ul>
  </div>
